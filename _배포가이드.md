# climbingstudios.com 배포 가이드

> Cloudflare에서 도메인을 구매했으므로, Cloudflare Pages로 호스팅하면 DNS가 자동 연결됩니다.

---

## 방법 A: Cloudflare Pages Direct Upload (가장 간단, GitHub 불필요)

### 1단계: Cloudflare 대시보드 접속
1. https://dash.cloudflare.com 로그인
2. 좌측 메뉴 **Workers & Pages** 클릭
3. **Create** → **Pages** 탭 → **Upload assets** 선택

### 2단계: 프로젝트 생성
1. 프로젝트 이름: `climbingstudios` 입력
2. **Upload** 클릭 → `Z:\hesedcorp\98_hesedAI\_작업_climbingstudios\` 폴더 안의 파일들을 **전부 드래그&드롭**:
   - `index.html`
   - `robots.txt`
   - `sitemap.xml`
   - `llms.txt`
   - `404.html`
   - `_headers`
   - `_redirects`
3. **Deploy site** 클릭

### 3단계: 커스텀 도메인 연결
1. 배포 완료 후 → **Custom domains** 탭
2. **Set up a custom domain** 클릭
3. `climbingstudios.com` 입력 → **Continue**
4. Cloudflare가 DNS 레코드를 **자동으로 추가** (같은 계정이므로)
5. **Activate domain** 클릭
6. SSL 인증서 자동 발급 (몇 분 소요)

### 4단계: www 리다이렉트 (선택)
1. `www.climbingstudios.com`도 추가하면 `_redirects` 파일에 의해 자동으로 apex로 301 리다이렉트

---

## 방법 B: GitHub 연동 (자동 배포, 업데이트 편함)

### 1단계: GitHub 레포 생성
```bash
cd "Z:/hesedcorp/98_hesedAI/_작업_climbingstudios"
git init
git add .
git commit -m "Initial: GEO-optimized landing page for 클라이밍 하이프렉스"
# GitHub에서 레포 생성 후:
git remote add origin https://github.com/hesedhouse/climbingstudios.git
git branch -M main
git push -u origin main
```

### 2단계: Cloudflare Pages 연결
1. https://dash.cloudflare.com → **Workers & Pages** → **Create**
2. **Pages** → **Connect to Git** → GitHub 계정 연결
3. 레포 `climbingstudios` 선택
4. Build settings:
   - **Framework preset**: None
   - **Build command**: (비워두기)
   - **Build output directory**: `/` (루트)
5. **Save and Deploy**

### 3단계: 커스텀 도메인 연결
- 방법 A의 3단계와 동일

---

## 배포 후 확인 체크리스트

- [ ] `https://climbingstudios.com` 접속 → 사이트 정상 표시
- [ ] `https://climbingstudios.com/robots.txt` → AI 크롤러 허용 확인
- [ ] `https://climbingstudios.com/sitemap.xml` → URL 목록 확인
- [ ] `https://climbingstudios.com/llms.txt` → AI 전용 정보 확인
- [ ] HTTPS 자물쇠 아이콘 확인 (SSL)
- [ ] 모바일 반응형 확인

## 배포 후 해야 할 것

### 검색엔진 등록
1. **Google Search Console**
   - https://search.google.com/search-console → 속성 추가
   - `climbingstudios.com` 도메인 입력
   - Cloudflare DNS에 TXT 레코드 추가 (자동 안내)
   - sitemap.xml 제출

2. **네이버 서치어드바이저**
   - https://searchadvisor.naver.com → 사이트 추가
   - HTML 메타태그 인증 (index.html에 `naver-site-verification` 값 채우기)
   - sitemap.xml 제출

3. **구글 비즈니스 프로필 (필수!)**
   - 하이프렉스 구글 비즈니스 프로필에 웹사이트 URL을 `https://climbingstudios.com` 으로 등록
   - 이것이 로컬 GEO의 핵심 — AI가 비즈니스 프로필의 웹사이트를 1차 출처로 참조

4. **네이버 스마트플레이스**
   - 웹사이트 URL을 `https://climbingstudios.com` 으로 업데이트
